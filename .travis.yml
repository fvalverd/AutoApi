language: python
python:
  - "2.7"
services:
  - mongodb
install:
  - python setup.py develop
before_script:
  - pip install coverage python-coveralls
script:
  - python setup.py run_tests -a '-sv'
after_script:
  - coveralls
