language: python
python:
  - 3.7
  - 3.6
  - 3.5
  - 3.4
  - 2.7
services:
  - mongodb
install:
  - python setup.py develop
before_script:
  - pip install coverage python-coveralls
script:
  - python setup.py test -a '-sv'
after_script:
  - coveralls
